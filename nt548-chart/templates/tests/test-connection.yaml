apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nt548-chart.fullname" . }}-test-connection"
  labels:
    {{- include "nt548-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nt548-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
